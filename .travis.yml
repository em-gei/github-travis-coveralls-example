cache:
  directories:
  - $HOME/.m2

language: java

matrix:
  include:
    - jdk: openjdk8
    - jdk: openjdk8
    - os: osx
      osx_image: xcode10.1

# skip installation step
install: true

script: "mvn -f myproject/pom.xml clean verify"

